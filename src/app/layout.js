import './globals.css'
export const metadata = { title: 'Meeting Action Extractor', description: 'Extract action items from meetings' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
